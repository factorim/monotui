# Monorepo - Web

Minimal React page.

## Quickstart

```sh
pnpm install
```

```sh
pnpm dev
```

Uses `PORT` from `.env` (defaults to `3000`). If `3000` is busy, either:

- set `PORT=3001 pnpm dev`, or
- edit `.env` to a free port.
